interface:
  display_name: "rvw Review Compose"
  short_description: "Minimize total review comprehension cost"
  default_prompt: "Use $rvw-review-compose to analyze this Pull Request and produce only the rvw review surfaces that materially lower total comprehension cost."
