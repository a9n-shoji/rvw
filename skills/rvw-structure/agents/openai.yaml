interface:
  display_name: "rvw Structure"
  short_description: "Map a behavior from its code entrypoint"
  default_prompt: "Use $rvw-structure to publish a bounded source-anchored behavior map in rvw."
