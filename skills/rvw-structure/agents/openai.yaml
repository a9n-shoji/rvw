interface:
  display_name: "rvw Structure"
  short_description: "Publish one bounded source-anchored behavior map"
  default_prompt: "Use $rvw-structure to publish one bounded source-anchored behavior map in rvw."
